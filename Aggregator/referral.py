def main():
    print("hi")
    # NOTE: Account for special tools w/ multiple tools. We need to check if we already reference the recipe, if so add ON... We can choose which referal link to use later
    # Ignore dashes in db?
    
if __name__ == "__main__":
    main()